#解析原始碼
import bs4
root = bs4.BeautifulSoup(data, "html.parser")
titles1 = root.find_all("div", class_="Codeforces Round")
titles2 = root.find_all("div", class_="Educational Codeforces Round")
print(titles1)

    file=open("./leetcode.json",'w')

def convert_time_string(time_string):
    if "Ended" in time_string:
        # 已經結束的比賽
        date_str = time_string.split("Ended")[1].strip()  # 從字符串中提取日期部分
        date = datetime.datetime.strptime(date_str, '%b %d, %Y')  # 將日期字符串轉換為 datetime 物件
    else:
        # 未來的比賽
        # 使用 dateutil.parser.parse 函數解析時間字符串
        # 請注意，這裡沒有提供一個基準日期，所以 parser.parse 函數將使用當前日期作為基準日期
        date = parser.parse(time_string)

    return date

# 測試
print(convert_time_string("Weekly Contest 346Sunday 2:30 AM UTC"))
print(convert_time_string("Weekly Contest 291EndedMay 1, 2022"))